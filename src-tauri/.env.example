# Tauri CLI dev watcher ignore filename (relative to src-tauri/).
# Copy this file to `.env` to apply locally. The shipped `.gitignore`
# already excludes the real `.env` so secrets stay out of the repo.
TAURI_CLI_WATCHER_IGNORE_FILENAME=.taurignore
